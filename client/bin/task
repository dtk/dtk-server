#!/usr/bin/env ruby
require File.expand_path('../commands/task', File.dirname(__FILE__))
include R8::Client

subcommand = ARGV[0]

args = Array.new

conn = Conn.new()

pp conn.task.apply(subcommand,*args)

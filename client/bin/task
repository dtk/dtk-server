#!/usr/bin/env ruby
require File.expand_path('../r8_client', File.dirname(__FILE__))
include R8::Client
load_command("task")


subcommand = ARGV[0]

args = Array.new

conn = Conn.new()

pp conn.task.apply(subcommand,*args)

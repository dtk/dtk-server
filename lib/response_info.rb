module DTK
  #TODO: stub so that anywhere in code (through use of Thread variable can append info about a response being processed, which
  # will be sent back in rest call
  class ResponseInfo
    def self.info(msg,*args)
    end
  end
end

For installation of DTK Client via DTK service it is needed to include following components on the node (in order specified):
1. common_user component which is used to install unix user on destination node
2. dtk_client component which will do the setup and installation of DTK Client
